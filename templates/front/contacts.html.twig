{% extends '/front/index.html.twig' %}
{% block content %}
    <section>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="page-header">
                        <h1>Адреса и контактная информация сети гостиниц «Hotel-hol»: </h1>
                    </div>
                </div>

                <div class="col-md-12">
                    {% for hotel in hotels %}
                    <div class="card caption">
                        <div class="row">
                            <div class="col-md-6">
                                <h2>Мини-отель «{{ hotel.title }}»</h2>
                                <p>Контактный телефон: <a href="tel: {{ hotel.phone }}">{{ hotel.phone }}</a><br>
                                    E-mail: <a href="mailto: {{ hotel.email }}"> {{ hotel.email }}</a><br>
                                    Адрес: {{ hotel.address }}<br>
                                    Метро: {{ hotel.subway }}
                                </p>
                                {{ hotel.address_description|raw }}
                            </div>
                            <div class="col-md-6">
                                <script type="text/javascript" charset="utf-8" src="{{ hotel.maps_link }}"></script>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
{% endblock %}